---
import type { Slide } from '../../lib/parser';

interface Props {
  slide: Slide;
}

const { slide } = Astro.props;
---

<div class="template-big">
  <div class="text">{slide.heading}</div>
</div>

<style>
  .template-big {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: clamp(24px, 6vmin, 180px);
    box-sizing: border-box;
  }
  .text {
    font-size: clamp(48px, 16vmin, 320px);
    font-weight: 900;
    letter-spacing: -0.04em;
    color: var(--theme-color);
    text-align: center;
    line-height: 0.95;
  }
</style>
