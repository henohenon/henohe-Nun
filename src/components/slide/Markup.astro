---
import { renderMarkup } from '../../lib/markup';

interface Props {
  body: string;
}

const { body } = Astro.props;
const html = await renderMarkup(body);
---

{html && <Fragment set:html={html} />}
